# Images 目录

把您的头像、项目封面图等放这里。

推荐结构：

```
assets/images/
├── avatar.jpg           # 个人头像（建议 400×400 像素）
├── favicon.png          # 网站图标（建议 32×32 像素）
└── projects/            # 项目封面图
    ├── project1.png
    └── project2.png
```

**说明：** 如果 `avatar.jpg` 不存在，主页会自动回退到 Gravatar 的默认占位头像。
